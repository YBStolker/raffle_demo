// TODO

// import { Test, TestingModule } from '@nestjs/testing';
// import { RaffleParticipantController } from './raffle-participant.controller';
// import { RaffleParticipantService } from './raffle-participant.service';

describe('RaffleParticipantController', () => {
  // let controller: RaffleParticipantController;

  // beforeEach(async () => {
  //   jest.clearAllMocks();

  //   const module: TestingModule = await Test.createTestingModule({
  //     controllers: [RaffleParticipantController],
  //     providers: [RaffleParticipantService],
  //   }).compile();

  //   controller = module.get<RaffleParticipantController>(
  //     RaffleParticipantController,
  //   );
  // });

  it('RaffleParticipantController.create', async () => {
    // expect(controller).toBeDefined();
    // const spy = spyOn(controller, 'create');
    // const result = await controller.create({});
    // expect(spy).toThrow();
    // console.log(result);
  });
});
